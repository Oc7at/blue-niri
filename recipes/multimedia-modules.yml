---

modules:
  - type: dnf
    repos:
      cleanup: true
      nonfree: negativo17
    replace:
      - from-repo: fedora-multimedia
        packages:
          - ffmpeg
    install:
      packages:
        - mpv@0.40.0
        - mpv-devel@0.40.0
        - steam
        - libavcodec
        - libatomic
